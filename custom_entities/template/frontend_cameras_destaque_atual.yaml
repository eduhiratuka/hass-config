- trigger:
      - platform: event
        event_type: "custom_events::highlited_camera_updated"
  select:
      - name: "Frontend: Cameras: Destaque Atual"
        unique_id: frontend_cameras_destaque_atual
        state: "{{ trigger.event.data.camera }}"
        options: "['Frente: Câmera da Garagem', 'Frente: Câmera da Lateral', 'Fundos: Câmera da Piscina','Fundos: Câmera do Quintal']"
        select_option:
            service: script.turn_on
            target:
                entity_id: script.casa_atualiza_camera_em_destaque
            data:
                variables:
                    camera_name: "{{ option }}"
